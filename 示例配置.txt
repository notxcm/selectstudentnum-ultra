# 抽学号 Ultra 配置示例
# 说明：本文件仅作格式参考。程序会自动保存为「抽学号Ultra.state.txt」。
# 普通使用请在程序里点「设置」直接填写，无需手动编辑本文件。
# 规则：每行一个键值；「范围」每行 = 起始,结束,步长,宽度(补零位数,0=不补)；
#       「手动」= 一个学号；「排除」= 一个不抽的学号。

[抽学号Ultra配置]
版本=1

# —— 学号范围（支持多行，数字范围自动展开）——
范围=1,50,1,0
范围=1001,1020,1,4

# —— 手动添加（每行一个，可含文字）——
手动=2023001
手动=A07

# —— 排除（每行一个，这些永远抽不到）——
排除=13
排除=47

# —— 其它设置 ——
动画毫秒=1200
界面缩放=1.00
主题=0
声音=1
不重复=1
数量=1
避免最近=0
